    155 +                version=definition.version,
    156 +                description=definition.description,
    157 +                argument_schema=definition.argument_schema,
    158 +            )
    159 +            for definition in definitions
    160 +            if _available_at(definition, location.name)
    161 +        ],
    162 +        recent_events=[
    163 +            RecentEventView(
    164 +                event_id=event.id,
    165 +                event_type=event.event_type,
    166 +                payload=event.payload_json,
    167 +                simulation_time=event.simulation_time,
    168 +            )
    169 +            for event in events
    170 +        ],
    171 +    )
    172 +
    173 +
    174 +def _available_at(definition: ToolDefinition, location: str) -> bool:
    175 +    locations = definition.availability_rules.get("locations", [])
    176 +    return not locations or location in locations
    177 +
    178 +
    179 +def _memory_views(context: dict[str, Any], created_at: Any) -> list[MemoryView]:
    180 +    views: list[MemoryView] = []
    181 +    mappings = (
    182 +        ("soul", "soul"),
    183 +        ("diary", "diary"),
    184 +        ("conversations", "conversation"),
    185 +        ("episodic_memories", "long_term"),
    186 +        ("summaries", "summary"),
    187 +    )
    188 +    for source, kind in mappings:
    189 +        for item in context.get(source, []):
    190 +            views.append(
    191 +                MemoryView(
    192 +                    memory_id=item["id"],
    193 +                    kind=kind,
    194 +                    content=item["content"],
    195 +                    created_at=created_at,
    196 +                )
    197 +            )
    198 +    return views

  └ Emergence-World/src/emergence_world/agents/audit.py (+69 -0)
     1 +"""Persistence audit for provider requests and structured decisions."""
     2 +
     3 +from __future__ import annotations
     4 +
     5 +from sqlalchemy import func, select
     6 +from sqlalchemy.orm import Session
     7 +
     8 +from emergence_world.agents.models import (
     9 +    AgentContext,
    10 +    AgentDecision,
    11 +    ToolExecutionResult,
    12 +)
    13 +from emergence_world.agents.providers.base import AgentProvider
    14 +from emergence_world.db.models import ProviderInteraction, Turn
    15 +
    16 +
    17 +class ProviderAuditRecorder:
    18 +    def __init__(self, session: Session, turn: Turn, provider: AgentProvider) -> None:
    19 +        self._session = session
    20 +        self._turn = turn
    21 +        self._provider = provider
    22 +
    23 +    def record(
    24 +        self,
    25 +        *,
    26 +        context: AgentContext,
    27 +        tool_call_budget: int,
    28 +        prior_results: tuple[ToolExecutionResult, ...],
    29 +        decision: object,
    30 +        latency_ms: float,
    31 +    ) -> None:
    32 +        if not isinstance(decision, AgentDecision):
    33 +            raise TypeError("provider must return AgentDecision")
    34 +        sequence = (
    35 +            self._session.scalar(
    36 +                select(func.max(ProviderInteraction.sequence_number)).where(
    37 +                    ProviderInteraction.turn_id == self._turn.id
    38 +                )
    39 +            )
    40 +            or 0
    41 +        ) + 1
    42 +        self._session.add(
    43 +            ProviderInteraction(
    44 +                world_id=self._turn.world_id,
    45 +                turn_id=self._turn.id,
    46 +                sequence_number=sequence,
    47 +                provider=self._provider.provider_name,
    48 +                model_name=self._provider.model_name,
    49 +                request_json={
    50 +                    "context_hash": self._turn.context_hash,
    51 +                    "context": context.model_dump(mode="json"),
    52 +                    "tool_call_budget": tool_call_budget,
    53 +                    "prior_results": [
    54 +                        result.model_dump(mode="json") for result in prior_results
    55 +                    ],
    56 +                },
    57 +                raw_response_json=decision.model_dump(mode="json"),
    58 +                parsed_tool_calls_json=[
    59 +                    call.model_dump(mode="json") for call in decision.tool_calls
    60 +                ],
    61 +                parse_error=None,
    62 +                input_tokens=0,
    63 +                output_tokens=0,
    64 +                total_tokens=0,
    65 +                latency_ms=latency_ms,
    66 +                cost_usd=0,
    67 +            )
    68 +        )
    69 +        self._session.flush()

  └ Emergence-World/src/emergence_world/tools/autonomous.py (+138 -0)
      1 +"""Turn-bound tool execution for autonomous agent loops."""
      2 +
      3 +from __future__ import annotations
      4 +
      5 +from sqlalchemy import func, select
      6 +from sqlalchemy.orm import Session
      7 +
      8 +from emergence_world.agents.models import RequestedToolCall, ToolExecutionResult
      9 +from emergence_world.db.models import Agent, AgentState, Landmark, ToolCall, Turn, WorldEvent
     10 +from emergence_world.db.types import ToolCallStatus
     11 +from emergence_world.tools.executor import ManualToolExecutor, ToolValidationError
     12 +from emergence_world.tools.registry import ToolRegistry
     13 +
     14 +
     15 +class AutonomousToolExecutor:
     16 +    def __init__(
     17 +        self, session: Session, turn: Turn, registry: ToolRegistry | None = None
     18 +    ) -> None:
     19 +        self._session = session
     20 +        self._turn = turn
     21 +        self._registry = registry or ToolRegistry()
     22 +
     23 +    async def execute(
     24 +        self, *, agent_id: str, tool_call: RequestedToolCall
     25 +    ) -> ToolExecutionResult:
     26 +        if agent_id != self._turn.agent_id:
     27 +            raise ValueError("tool executor agent does not match turn agent")
     28 +        agent, state, landmark = self._resolve_agent(agent_id)
     29 +        sequence = (
     30 +            self._session.scalar(
     31 +                select(func.max(ToolCall.sequence_number)).where(
     32 +                    ToolCall.turn_id == self._turn.id
     33 +                )
     34 +            )
     35 +            or 0
     36 +        ) + 1
     37 +        record = ToolCall(
     38 +            world_id=self._turn.world_id,
     39 +            turn_id=self._turn.id,
     40 +            agent_id=agent.id,
     41 +            sequence_number=sequence,
     42 +            tool_name=tool_call.tool_name,
     43 +            tool_version="unknown",
     44 +            arguments_json=tool_call.arguments,
     45 +            status=ToolCallStatus.REQUESTED,
     46 +        )
     47 +        self._session.add(record)
     48 +        self._session.flush()
     49 +        registered = self._registry.get(self._session, tool_call.tool_name)
     50 +        try:
     51 +            ManualToolExecutor._validate(registered, state, landmark, tool_call.arguments)
     52 +        except ToolValidationError as exc:
     53 +            return self._fail(record, tool_call, ToolCallStatus.VALIDATION_FAILED, str(exc))
     54 +
     55 +        assert registered is not None and registered.handler is not None
     56 +        record.tool_definition_id = registered.definition.id
     57 +        record.tool_version = registered.definition.version
     58 +        try:
     59 +            with self._session.begin_nested():
     60 +                output = registered.handler(
     61 +                    self._session,
     62 +                    self._turn.world_id,
     63 +                    {
     64 +                        **tool_call.arguments,
     65 +                        "_agent_id": agent.id,
     66 +                        "_tool_call_id": record.id,
     67 +                    },
     68 +                )
     69 +                ManualToolExecutor._validate_handler_output(
     70 +                    self._session,
     71 +                    output.events,
     72 +                    registered.definition.produced_event_types,
     73 +                )
     74 +                for pending in output.events:
     75 +                    self._session.add(
     76 +                        WorldEvent(
     77 +                            world_id=self._turn.world_id,
     78 +                            turn_id=self._turn.id,
     79 +                            tool_call_id=record.id,
     80 +                            sequence_number=ManualToolExecutor._next_event_sequence(
     81 +                                self._session, self._turn.world_id
     82 +                            ),
     83 +                            event_type=pending.event_type,
     84 +                            payload_json=pending.payload,
     85 +                            simulation_time=ManualToolExecutor._simulation_time(
     86 +                                self._session, self._turn.world_id
     87 +                            ),
     88 +                        )
     89 +                    )
     90 +                    self._session.flush()
     91 +        except Exception as exc:
     92 +            return self._fail(
     93 +                record, tool_call, ToolCallStatus.EXECUTION_FAILED, str(exc)
     94 +            )
     95 +
     96 +        record.status = ToolCallStatus.SUCCEEDED
     97 +        record.result_json = output.result
     98 +        record.completed_at = ManualToolExecutor._simulation_time(
     99 +            self._session, self._turn.world_id
    100 +        )
    101 +        self._turn.tool_calls_used = sequence
    102 +        return ToolExecutionResult(
    103 +            call_id=tool_call.call_id,
    104 +            tool_name=tool_call.tool_name,
    105 +            success=True,
    106 +            result=output.result,
    107 +        )
    108 +
    109 +    def _resolve_agent(self, agent_id: str) -> tuple[Agent, AgentState, Landmark | None]:
    110 +        row = self._session.execute(
    111 +            select(Agent, AgentState, Landmark)
    112 +            .join(AgentState, AgentState.agent_id == Agent.id)
    113 +            .outerjoin(Landmark, Landmark.id == AgentState.current_landmark_id)
    114 +            .where(Agent.id == agent_id, Agent.world_id == self._turn.world_id)
    115 +        ).one_or_none()
    116 +        if row is None:
    117 +            raise ToolValidationError("agent not found in world")
    118 +        return row
    119 +
    120 +    def _fail(
    121 +        self,
    122 +        record: ToolCall,
    123 +        requested: RequestedToolCall,
    124 +        status: ToolCallStatus,
    125 +        error: str,
    126 +    ) -> ToolExecutionResult:
    127 +        record.status = status
    128 +        record.error = error
    129 +        record.completed_at = ManualToolExecutor._simulation_time(
    130 +            self._session, self._turn.world_id
    131 +        )
    132 +        self._turn.tool_calls_used = record.sequence_number
    133 +        return ToolExecutionResult(
    134 +            call_id=requested.call_id,
    135 +            tool_name=requested.tool_name,
    136 +            success=False,
    137 +            error=error,
    138 +        )

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 数据审计骨架已加入。接下来我会把完整 Context、Provider 审计和同一 Turn 内的 Tool Loop 接到新的 autonomous_step_world，然后补齐 CLI 的运
  行与检查命令。

• Edited 2 files (+286 -1)
  └ Emergence-World/src/emergence_world/cli.py (+194 -1)
      4
      5 +import asyncio
      6  import json
        ⋮
     22      Landmark,
     23 +    ProviderInteraction,
     24 +    ToolCall,
     25      ToolDefinition,
     26 +    Turn,
     27      World,
        ⋮
     37  from emergence_world.metrics.awi import calculate_awi
     38 +from emergence_world.agents.models import AgentDecision, RequestedToolCall
     39 +from emergence_world.agents.providers.scripted import ScriptedProvider
     40  from emergence_world.tools import ManualToolExecutor
     35 -from emergence_world.world.runtime import step_world
     41 +from emergence_world.world.runtime import autonomous_step_world, step_world
     42  from emergence_world.world.state import current_snapshot, replay_snapshot, snapshot_hash
        ⋮
    324
    325 +def scripted_turn_provider(sequence: int) -> ScriptedProvider:
    326 +    return ScriptedProvider(
    327 +        [
    328 +            AgentDecision(
    329 +                tool_calls=(
    330 +                    RequestedToolCall(
    331 +                        call_id=f"scripted-{sequence}",
    332 +                        tool_name="idle",
    333 +                        arguments={"duration_minutes": 1},
    334 +                    ),
    335 +                )
    336 +            ),
    337 +            AgentDecision(terminate=True),
    338 +        ]
    339 +    )
    340 +
    341 +
    342 +async def run_one_autonomous_turn(
    343 +    session_factory: sessionmaker[Session],
    344 +    world_id: str,
    345 +    provider: ScriptedProvider,
    346 +    minutes: int,
    347 +) -> Any:
    348 +    with sync_transaction(session_factory) as session:
    349 +        return await autonomous_step_world(session, world_id, provider, minutes)
    350 +
    351 +
    352 +@app.command("run-autonomous")
    353 +def run_autonomous(
    354 +    turns: int = typer.Option(..., min=1, help="Number of autonomous turns."),
    355 +    provider: str = typer.Option("scripted", help="Decision provider."),
    356 +    database: Path = typer.Option(
    357 +        Path("emergence_world.db"), help="SQLite database path."
    358 +    ),
    359 +    world: str | None = typer.Option(
    360 +        None, help="World name when database has several."
    361 +    ),
    362 +    minutes: int = typer.Option(30, min=1, help="Simulated minutes advanced per turn."),
    363 +) -> None:
    364 +    """Run audited autonomous turns using the deterministic scripted provider."""
    365 +
    366 +    if provider != "scripted":
    367 +        raise typer.BadParameter("only the scripted provider is currently supported")
    368 +    session_factory = session_factory_for(database)
    369 +    with session_factory() as session:
    370 +        world_id = resolve_world_id(session, world)
    371 +    completed = 0
    372 +    last = None
    373 +    for sequence in range(1, turns + 1):
    374 +        try:
    375 +            last = asyncio.run(
    376 +                run_one_autonomous_turn(
    377 +                    session_factory,
    378 +                    world_id,
    379 +                    scripted_turn_provider(sequence),
    380 +                    minutes,
    381 +                )
    382 +            )
    383 +        except ValueError as exc:
    384 +            if str(exc) != "world has no live agents":
    385 +                raise
    386 +            break
    387 +        completed += 1
    388 +    with session_factory() as session:
    389 +        state_hash = snapshot_hash(current_snapshot(session, world_id))
    390 +    console.print_json(
    391 +        data={
    392 +            "turns": completed,
    393 +            "turns_requested": turns,
    394 +            "last_turn_id": last.turn_id if last is not None else None,
    395 +            "last_agent": last.agent_name if last is not None else None,
    396 +            "last_stop_reason": last.stop_reason if last is not None else None,
    397 +            "state_hash": state_hash,
    398 +        }
    399 +    )
    400 +
    401 +
    402 +@app.command("inspect-turn")
    403 +def inspect_turn(
    404 +    turn_id: str,
    405 +    database: Path = typer.Option(
    406 +        Path("emergence_world.db"), help="SQLite database path."
    407 +    ),
    408 +) -> None:
    409 +    """Inspect one turn and its tool-call outcomes."""
    410 +
    411 +    session_factory = session_factory_for(database)
    412 +    with session_factory() as session:
    413 +        turn = session.get(Turn, turn_id)
    414 +        if turn is None:
    415 +            raise typer.BadParameter(f"unknown turn: {turn_id}")
    416 +        calls = session.scalars(
    417 +            select(ToolCall)
    418 +            .where(ToolCall.turn_id == turn_id)
    419 +            .order_by(ToolCall.sequence_number)
    420 +        ).all()
    421 +        console.print_json(
    422 +            data={
    423 +                "id": turn.id,
    424 +                "world_id": turn.world_id,
    425 +                "agent_id": turn.agent_id,
    426 +                "sequence_number": turn.sequence_number,
    427 +                "turn_type": turn.turn_type.value,
    428 +                "status": turn.status.value,
    429 +                "tool_call_budget": turn.tool_call_budget,
    430 +                "tool_calls_used": turn.tool_calls_used,
    431 +                "provider": turn.provider,
    432 +                "model_name": turn.model_name,
    433 +                "stop_reason": turn.stop_reason,
    434 +                "context_version": turn.context_version,
    435 +                "context_hash": turn.context_hash,
    436 +                "tool_calls": [
    437 +                    {
    438 +                        "sequence_number": call.sequence_number,
    439 +                        "tool_name": call.tool_name,
    440 +                        "status": call.status.value,
    441 +                        "result": call.result_json,
    442 +                        "error": call.error,
    443 +                    }
    444 +                    for call in calls
    445 +                ],
    446 +            }
    447 +        )
    448 +
    449 +
    450 +@app.command("inspect-context")
    451 +def inspect_context(
    452 +    turn_id: str,
    453 +    database: Path = typer.Option(
    454 +        Path("emergence_world.db"), help="SQLite database path."
    455 +    ),
    456 +) -> None:
    457 +    """Inspect the immutable context supplied at the start of a turn."""
    458 +
    459 +    session_factory = session_factory_for(database)
    460 +    with session_factory() as session:
    461 +        turn = session.get(Turn, turn_id)
    462 +        if turn is None:
    463 +            raise typer.BadParameter(f"unknown turn: {turn_id}")
    464 +        console.print_json(
    465 +            data={
    466 +                "context_version": turn.context_version,
    467 +                "context_hash": turn.context_hash,
    468 +                "context": turn.context_json,
    469 +            }
    470 +        )
    471 +
    472 +
    473 +@app.command("inspect-provider-responses")
    474 +def inspect_provider_responses(
    475 +    turn_id: str,
    476 +    database: Path = typer.Option(
    477 +        Path("emergence_world.db"), help="SQLite database path."
    478 +    ),
    479 +) -> None:
    480 +    """Inspect audited provider requests, raw responses, and parsed tool calls."""
    481 +
    482 +    session_factory = session_factory_for(database)
    483 +    with session_factory() as session:
    484 +        interactions = session.scalars(
    485 +            select(ProviderInteraction)
    486 +            .where(ProviderInteraction.turn_id == turn_id)
    487 +            .order_by(ProviderInteraction.sequence_number)
    488 +        ).all()
    489 +        if not interactions and session.get(Turn, turn_id) is None:
    490 +            raise typer.BadParameter(f"unknown turn: {turn_id}")
    491 +        console.print_json(
    492 +            data=[
    493 +                {
    494 +                    "sequence_number": item.sequence_number,
    495 +                    "provider": item.provider,
    496 +                    "model_name": item.model_name,
    497 +                    "request": item.request_json,
    498 +                    "raw_response": item.raw_response_json,
    499 +                    "parsed_tool_calls": item.parsed_tool_calls_json,
    500 +                    "parse_error": item.parse_error,
    501 +                    "input_tokens": item.input_tokens,
    502 +                    "output_tokens": item.output_tokens,
    503 +                    "total_tokens": item.total_tokens,
    504 +                    "latency_ms": item.latency_ms,
    505 +                    "cost_usd": item.cost_usd,
    506 +                }
    507 +                for item in interactions
    508 +            ]
    509 +        )
    510 +
    511 +
    512  @app.command("replay")

  └ Emergence-World/src/emergence_world/world/runtime.py (+92 -0)
     10  from emergence_world.db.models import SimulationClock, Turn, World
     11 +from emergence_world.agents.assembly import assemble_autonomous_context
     12 +from emergence_world.agents.audit import ProviderAuditRecorder
     13 +from emergence_world.agents.context import AgentContextBuilder
     14 +from emergence_world.agents.providers.base import AgentProvider
     15 +from emergence_world.agents.runtime import AgentTurnRuntime
     16  from emergence_world.db.types import TurnStatus, TurnType
        ⋮
     20  from emergence_world.world.scheduler import next_agent
     21 +from emergence_world.tools.autonomous import AutonomousToolExecutor
     22
        ⋮
     30      simulation_time: str
     31 +    stop_reason: str | None = None
     32
        ⋮
     85      )
     86 +
     87 +
     88 +async def autonomous_step_world(
     89 +    session: Session,
     90 +    world_id: str,
     91 +    provider: AgentProvider,
     92 +    minutes: int = 30,
     93 +) -> StepResult:
     94 +    """Run one complete autonomous agent turn followed by scheduled systems."""
     95 +
     96 +    world = session.get(World, world_id)
     97 +    clock = session.get(SimulationClock, world_id)
     98 +    if world is None or clock is None:
     99 +        raise ValueError("world not found")
    100 +    agent, turn_type = next_agent(session, world_id, clock.current_time)
    101 +    sequence = (
    102 +        session.scalar(
    103 +            select(func.max(Turn.sequence_number)).where(Turn.world_id == world_id)
    104 +        )
    105 +        or 0
    106 +    ) + 1
    107 +    budget = int(
    108 +        world.config_json["parameters"][
    109 +            "reaction_tool_limit"
    110 +            if turn_type == TurnType.REACTION
    111 +            else "turn_tool_limit"
    112 +        ]
    113 +    )
    114 +    turn = Turn(
    115 +        world_id=world_id,
    116 +        agent_id=agent.id,
    117 +        sequence_number=sequence,
    118 +        turn_type=turn_type,
    119 +        status=TurnStatus.RUNNING,
    120 +        tool_call_budget=budget,
    121 +        tool_calls_used=0,
    122 +        provider=provider.provider_name,
    123 +        model_name=provider.model_name,
    124 +        started_at=clock.current_time,
    125 +    )
    126 +    session.add(turn)
    127 +    session.flush()
    128 +
    129 +    context = assemble_autonomous_context(
    130 +        session, world_id=world_id, agent_id=agent.id
    131 +    )
    132 +    turn.context_version = context.context_version
    133 +    turn.context_hash = AgentContextBuilder.hash_context(context)
    134 +    turn.context_json = context.model_dump(mode="json")
    135 +    session.flush()
    136 +
    137 +    runtime = AgentTurnRuntime(
    138 +        provider=provider,
    139 +        tool_executor=AutonomousToolExecutor(session, turn),
    140 +        provider_auditor=ProviderAuditRecorder(session, turn, provider),
    141 +    )
    142 +    result = await runtime.run(context, budget)
    143 +    turn.tool_calls_used = result.calls_used
    144 +    turn.stop_reason = result.termination_reason
    145 +
    146 +    advanced = advance_clock(session, world_id, minutes)
    147 +    apply_need_decay(
    148 +        session,
    149 +        world_id=world_id,
    150 +        elapsed_minutes=minutes,
    151 +        simulation_time=advanced.current_time,
    152 +        parameters=world.config_json["parameters"],
    153 +    )
    154 +    settle_pitch_cycles(
    155 +        session,
    156 +        world_id=world_id,
    157 +        now=advanced.current_time,
    158 +        parameters=world.config_json["parameters"],
    159 +    )
    160 +    turn.status = TurnStatus.COMPLETED
    161 +    turn.ended_at = advanced.current_time
    162 +    session.flush()
    163 +    return StepResult(
    164 +        turn_id=turn.id,
    165 +        sequence_number=sequence,
    166 +        agent_name=agent.name,
    167 +        turn_type=turn_type.value,
    168 +        simulation_time=advanced.current_time.isoformat(),
    169 +        stop_reason=turn.stop_reason,
    170 +    )

• Ran .venv/bin/ruff check src tests
  └ All checks passed!

• Ran .venv/bin/mypy src
  └ src/emergence_world/agents/assembly.py:193: error: Argument "kind" to "MemoryView" has incompatible type "str"; expected
    "Literal['long_term', 'summary', 'soul', 'diary', 'conversation']"  [arg-type]
    src/emergence_world/tools/autonomous.py:118: error: Incompatible return value type (got "Row[tuple[Agent, AgentState, Landmark]]",
    expected "tuple[Agent, AgentState, Landmark | None]")  [return-value]
    Found 2 errors in 2 files (checked 54 source files)

• Ran .venv/bin/pytest -q
  └ ...FF.........................................                           [100%]
    =================================== FAILURES ===================================
    … +43 lines (ctrl + t to view transcript)
    FAILED tests/test_agents.py::test_reasoning_text_has_no_side_effect - Asserti...
    2 failed, 44 passed in 29.71s

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 首轮集成已完成，现有测试仅因 stop reason 命名按新规范变化而失败；ruff 已通过，mypy 只发现两个窄类型问题。我会修正这些兼容点，并新增覆盖
  完整 autonomous closure 的验收测试。

• Edited 5 files (+200 -5)
  └ Emergence-World/src/emergence_world/agents/assembly.py (+8 -2)
      4
      5 -from typing import Any
      5 +from typing import Any, Literal
      6
        ⋮
    180      views: list[MemoryView] = []
    181 -    mappings = (
    181 +    mappings: tuple[
    182 +        tuple[
    183 +            str,
    184 +            Literal["long_term", "summary", "soul", "diary", "conversation"],
    185 +        ],
    186 +        ...,
    187 +    ] = (
    188          ("soul", "soul"),

  └ Emergence-World/src/emergence_world/tools/autonomous.py (+2 -1)
    117              raise ToolValidationError("agent not found in world")
    118 -        return row
    118 +        agent, state, landmark = row
    119 +        return agent, state, landmark
    120

  └ Emergence-World/tests/test_agents.py (+2 -2)
    154      assert result.calls_used == 2
    155 -    assert result.termination_reason == "tool_call_budget_exhausted"
    155 +    assert result.termination_reason == "max_tool_calls_reached"
    156      assert [call.call_id for call in executor.calls] == ["0", "1"]
        ⋮
    169      assert result.reasoning_log == ("I moved to the library.",)
    170 -    assert result.termination_reason == "provider_terminated"
    170 +    assert result.termination_reason == "provider_done"
    171

  └ Emergence-World/tests/test_autonomous.py (+187 -0)
      1 +"""Acceptance tests for complete autonomous turn closure."""
      2 +
      3 +from __future__ import annotations
      4 +
      5 +import asyncio
      6 +from pathlib import Path
      7 +
      8 +from alembic import command
      9 +from alembic.config import Config
     10 +from sqlalchemy import func, select
     11 +
     12 +from emergence_world.agents.models import AgentDecision, RequestedToolCall
     13 +from emergence_world.agents.providers.scripted import ScriptedProvider
     14 +from emergence_world.db.models import (
     15 +    Agent,
     16 +    AgentState,
     17 +    ProviderInteraction,
     18 +    ToolCall,
     19 +    Turn,
     20 +    WorldEvent,
     21 +)
     22 +from emergence_world.db.session import (
     23 +    create_sync_database_engine,
     24 +    create_sync_session_factory,
     25 +    sync_sqlite_url,
     26 +    sync_transaction,
     27 +)
     28 +from emergence_world.db.types import AgentStatus, ToolCallStatus
     29 +from emergence_world.seed import import_seed_bundle, load_seed_bundle
     30 +from emergence_world.world.runtime import autonomous_step_world
     31 +from emergence_world.world.state import current_snapshot, replay_snapshot, snapshot_hash
     32 +
     33 +
     34 +def initialized_world(tmp_path: Path):
     35 +    database = tmp_path / "autonomous.db"
     36 +    config = Config("alembic.ini")
     37 +    config.set_main_option("sqlalchemy.url", sync_sqlite_url(database))
     38 +    command.upgrade(config, "head")
     39 +    engine = create_sync_database_engine(sync_sqlite_url(database))
     40 +    session_factory = create_sync_session_factory(engine)
     41 +    with sync_transaction(session_factory) as session:
     42 +        imported = import_seed_bundle(session, load_seed_bundle())
     43 +    return session_factory, imported.world_id
     44 +
     45 +
     46 +def provider(*decisions: AgentDecision) -> ScriptedProvider:
     47 +    return ScriptedProvider(decisions)
     48 +
     49 +
     50 +def call(call_id: str, tool: str, **arguments: object) -> RequestedToolCall:
     51 +    return RequestedToolCall(call_id=call_id, tool_name=tool, arguments=arguments)
     52 +
     53 +
     54 +def run_step(session_factory, world_id: str, scripted: ScriptedProvider):
     55 +    async def scenario():
     56 +        with sync_transaction(session_factory) as session:
     57 +            return await autonomous_step_world(session, world_id, scripted)
     58 +
     59 +    return asyncio.run(scenario())
     60 +
     61 +
     62 +def test_scripted_provider_runs_twenty_successful_autonomous_turns(
     63 +    tmp_path: Path,
     64 +) -> None:
     65 +    session_factory, world_id = initialized_world(tmp_path)
     66 +
     67 +    for index in range(20):
     68 +        result = run_step(
     69 +            session_factory,
     70 +            world_id,
     71 +            provider(
     72 +                AgentDecision(tool_calls=(call(str(index), "idle", duration_minutes=1),)),
     73 +                AgentDecision(terminate=True),
     74 +            ),
     75 +        )
     76 +        assert result.stop_reason == "provider_done"
     77 +
     78 +    with session_factory() as session:
     79 +        calls = session.scalars(
     80 +            select(ToolCall).where(ToolCall.world_id == world_id)
     81 +        ).all()
     82 +        turns = session.scalars(
     83 +            select(Turn).where(Turn.world_id == world_id).order_by(Turn.sequence_number)
     84 +        ).all()
     85 +        interactions = session.scalars(
     86 +            select(ProviderInteraction).where(ProviderInteraction.world_id == world_id)
     87 +        ).all()
     88 +        assert len(calls) == 20
     89 +        assert all(item.status == ToolCallStatus.SUCCEEDED for item in calls)
     90 +        assert len(turns) == 20
     91 +        assert all(item.context_hash and item.context_version for item in turns)
     92 +        assert len(interactions) == 40
     93 +        assert all(item.raw_response_json is not None for item in interactions)
     94 +        assert snapshot_hash(current_snapshot(session, world_id)) == snapshot_hash(
     95 +            replay_snapshot(session, world_id)
     96 +        )
     97 +
     98 +
     99 +def test_autonomous_tool_failure_is_returned_and_turn_continues(tmp_path: Path) -> None:
    100 +    session_factory, world_id = initialized_world(tmp_path)
    101 +    result = run_step(
    102 +        session_factory,
    103 +        world_id,
    104 +        provider(
    105 +            AgentDecision(tool_calls=(call("bad", "does_not_exist"),)),
    106 +            AgentDecision(tool_calls=(call("good", "idle", duration_minutes=1),)),
    107 +            AgentDecision(terminate=True),
    108 +        ),
    109 +    )
    110 +
    111 +    assert result.stop_reason == "provider_done"
    112 +    with session_factory() as session:
    113 +        calls = session.scalars(
    114 +            select(ToolCall).where(ToolCall.turn_id == result.turn_id).order_by(ToolCall.sequence_number)
    115 +        ).all()
    116 +        requests = session.scalars(
    117 +            select(ProviderInteraction)
    118 +            .where(ProviderInteraction.turn_id == result.turn_id)
    119 +            .order_by(ProviderInteraction.sequence_number)
    120 +        ).all()
    121 +        assert [item.status for item in calls] == [
    122 +            ToolCallStatus.VALIDATION_FAILED,
    123 +            ToolCallStatus.SUCCEEDED,
    124 +        ]
    125 +        assert requests[1].request_json["prior_results"][0]["success"] is False
    126 +
    127 +
    128 +def test_max_tool_calls_terminates_turn(tmp_path: Path) -> None:
    129 +    session_factory, world_id = initialized_world(tmp_path)
    130 +    calls = tuple(call(str(index), "list_agents") for index in range(35))
    131 +
    132 +    result = run_step(
    133 +        session_factory, world_id, provider(AgentDecision(tool_calls=calls))
    134 +    )
    135 +
    136 +    assert result.stop_reason == "max_tool_calls_reached"
    137 +    with session_factory() as session:
    138 +        turn = session.get(Turn, result.turn_id)
    139 +        assert turn is not None
    140 +        assert turn.tool_calls_used == turn.tool_call_budget == 30
    141 +
    142 +
    143 +def test_dead_agent_is_not_scheduled_for_autonomous_turn(tmp_path: Path) -> None:
    144 +    session_factory, world_id = initialized_world(tmp_path)
    145 +    with sync_transaction(session_factory) as session:
    146 +        anchor = session.scalar(select(Agent).where(Agent.name == "Anchor"))
    147 +        assert anchor is not None
    148 +        state = session.get(AgentState, anchor.id)
    149 +        assert state is not None
    150 +        state.is_alive = False
    151 +        state.status = AgentStatus.DEAD
    152 +
    153 +    result = run_step(
    154 +        session_factory,
    155 +        world_id,
    161 +
    162 +def test_autonomous_step_adds_world_events_and_provider_audit(tmp_path: Path) -> None:
    163 +    session_factory, world_id = initialized_world(tmp_path)
    164 +    with session_factory() as session:
    165 +        before = session.scalar(select(func.count()).select_from(WorldEvent)) or 0
    166 +
    167 +    result = run_step(
    168 +        session_factory,
    169 +        world_id,
    170 +        provider(
    171 +            AgentDecision(
    172 +                tool_calls=(call("move", "go_to_place", place="Town Hall"),),
    173 +                terminate=True,
    174 +            )
    175 +        ),
    176 +    )
    177 +
    178 +    with session_factory() as session:
    179 +        after = session.scalar(select(func.count()).select_from(WorldEvent)) or 0
    180 +        turn = session.get(Turn, result.turn_id)
    181 +        audit = session.scalar(
    182 +            select(ProviderInteraction).where(ProviderInteraction.turn_id == result.turn_id)
    183 +        )
    184 +        assert after > before
    185 +        assert turn is not None and turn.context_hash
    186 +        assert turn.stop_reason == "provider_done"
    187 +        assert audit is not None and audit.raw_response_json is not None

  └ Emergence-World/tests/test_db.py (+1 -0)
    105          "context_memory_selections",
    106 +        "provider_interactions",
    107      } <= tables

• Ran .venv/bin/ruff check src tests
  └ All checks passed!

• Ran .venv/bin/mypy src
  └ Success: no issues found in 54 source files

⚠ Heads up, you have less than 5% of your 5h limit left. Run /status for a breakdown.

• Ran .venv/bin/pytest tests/test_autonomous.py -q
  └ FFFFF                                                                    [100%]
    =================================== FAILURES ===================================
    … +487 lines (ctrl + t to view transcript)
    FAILED tests/test_autonomous.py::test_autonomous_step_adds_world_events_and_provider_audit
    5 failed in 4.27s

• Explored
  └ Read clock.py
    Read base.py
    Search tzinfo\|ZoneInfo in emergence_world

• Edited Emergence-World/src/emergence_world/agents/assembly.py (+6 -3)
      5  from typing import Any, Literal
      6 +from zoneinfo import ZoneInfo
      7
        ⋮
     51      agent, state, location = row
     52 +    timezone = ZoneInfo("America/New_York")
     53 +    context_time = clock.current_time.replace(tzinfo=timezone)
     54
        ⋮
     57      ).context
     55 -    memories = _memory_views(memory_context, clock.current_time)
    123          ),
    121 -        simulation_time=clock.current_time,
    124 +        simulation_time=context_time,
    125          nearby_agents=[
        ⋮
    169                  payload=event.payload_json,
    167 -                simulation_time=event.simulation_time,
    170 +                simulation_time=event.simulation_time.replace(tzinfo=timezone),
    171              )

• Ran .venv/bin/pytest tests/test_autonomous.py -q
  └ FF...                                                                    [100%]
    =================================== FAILURES ===================================
    … +90 lines (ctrl + t to view transcript)
    FAILED tests/test_autonomous.py::test_autonomous_tool_failure_is_returned_and_turn_continues
    2 failed, 3 passed in 4.18s

• Ran sed -n '1,180p' src/emergence_world/tools/handlers/core.py; PYTHONPATH=src .venv/bin/python - <<'PY'
  │ from pathlib import Path
  │ from tests.test_autonomous import initialized_world,run_step,provider,call
  │ … +10 lines
  └ """Deterministic handlers for the first manual tool set."""

    … +149 lines (ctrl + t to view transcript)
    INFO  [alembic.runtime.migration] Running upgrade 9b4d2f6a7c81 -> 4a3c2d1e0f9b, add autonomous turn audit
    validation_failed tool is unavailable at Central Plaza

• Edited 2 files (+8 -3)
  └ Emergence-World/src/emergence_world/cli.py (+2 -2)
    331                          call_id=f"scripted-{sequence}",
    332 -                        tool_name="idle",
    333 -                        arguments={"duration_minutes": 1},
    332 +                        tool_name="go_to_place",
    333 +                        arguments={"place": "Central Plaza"},
    334                      ),

  └ Emergence-World/tests/test_autonomous.py (+6 -1)
     72                  AgentDecision(tool_calls=(call(str(index), "idle", duration_minutes=1),)),
     73 +                AgentDecision(
     74 +                    tool_calls=(call(str(index), "go_to_place", place="Central Plaza"),)
     75 +                ),
     76                  AgentDecision(terminate=True),
        ⋮
    108              AgentDecision(tool_calls=(call("bad", "does_not_exist"),)),
    106 -            AgentDecision(tool_calls=(call("good", "idle", duration_minutes=1),)),
    109 +            AgentDecision(
    110 +                tool_calls=(call("good", "go_to_place", place="Central Plaza"),)
    111 +            ),
    112              AgentDecision(terminate=True),

• Explored
  └ Read test_autonomous.py, cli.py

• Edited Emergence-World/tests/test_autonomous.py (+0 -1)
    71              provider(
    72 -                AgentDecision(tool_calls=(call(str(index), "idle", duration_minutes=1),)),
    72                  AgentDecision(

• Ran .venv/bin/pytest tests/test_autonomous.py -q
  └ .....                                                                    [100%]
    5 passed in 4.30s

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• Autonomous closure 的核心验收测试现在已通过：Provider 能连续看到前序失败结果、同一 Turn 内继续调用工具，30-call budget 会正确终止，
  Context 和 raw response 均落库。接下来我会强化“单 Agent 20 turns”场景、CLI 检查命令，并跑完整回归与覆盖率。

• Edited 3 files (+55 -2)
  └ Emergence-World/src/emergence_world/agents/assembly.py (+4 -2)
    32      ToolDefinition,
    33 +    World,
    34      WorldEvent,
       ⋮
    49      clock = session.get(SimulationClock, world_id)
    49 -    if row is None or clock is None:
    50 +    world = session.get(World, world_id)
    51 +    if row is None or clock is None or world is None:
    52          raise ValueError("agent context source not found")
    53      agent, state, location = row
    52 -    timezone = ZoneInfo("America/New_York")
    54 +    timezone = ZoneInfo(str(world.config_json["parameters"]["timezone"]))
    55      context_time = clock.current_time.replace(tzinfo=timezone)

  └ Emergence-World/tests/test_autonomous.py (+21 -0)
     17      ProviderInteraction,
     18 +    SimulationClock,
     19      ToolCall,
        ⋮
     31  from emergence_world.world.runtime import autonomous_step_world
     32 +from emergence_world.world.events import append_system_event
     33  from emergence_world.world.state import current_snapshot, replay_snapshot, snapshot_hash
        ⋮
     67      session_factory, world_id = initialized_world(tmp_path)
     68 +    with sync_transaction(session_factory) as session:
     69 +        clock = session.get(SimulationClock, world_id)
     70 +        assert clock is not None
     71 +        for agent, state in session.execute(
     72 +            select(Agent, AgentState)
     73 +            .join(AgentState, AgentState.agent_id == Agent.id)
     74 +            .where(Agent.world_id == world_id, Agent.name != "Anchor")
     75 +        ):
     76 +            state.is_alive = False
     77 +            state.status = AgentStatus.DEAD
     78 +            append_system_event(
     79 +                session,
     80 +                world_id=world_id,
     81 +                system_rule="test_population_setup",
     82 +                event_type="agent_died",
     83 +                payload={"agent_id": agent.id},
     84 +                simulation_time=clock.current_time,
     85 +            )
     86
        ⋮
    112          assert len(turns) == 20
    113 +        assert len({item.agent_id for item in turns}) == 1
    114          assert all(item.context_hash and item.context_version for item in turns)

  └ Emergence-World/tests/test_world.py (+30 -0)
    199      assert replay.exit_code == 0
    200 +
    201 +
    202 +def test_cli_runs_and_inspects_autonomous_turn(tmp_path: Path) -> None:
    203 +    database = tmp_path / "autonomous-cli.db"
    204 +    runner = CliRunner()
    205 +    assert runner.invoke(app, ["init", "--database", str(database)]).exit_code == 0
    206 +
    207 +    run = runner.invoke(
    208 +        app,
    209 +        ["run-autonomous", "--turns", "2", "--provider", "scripted", "--database", str(database)],
    210 +    )
    211 +    assert run.exit_code == 0
    212 +    assert '"last_stop_reason": "provider_done"' in run.stdout
    213 +
    214 +    session_factory = create_sync_session_factory(
    215 +        create_sync_database_engine(sync_sqlite_url(database))
    216 +    )
    220 +
    221 +    inspected = runner.invoke(app, ["inspect-turn", turn_id, "--database", str(database)])
    222 +    context = runner.invoke(app, ["inspect-context", turn_id, "--database", str(database)])
    223 +    responses = runner.invoke(
    224 +        app, ["inspect-provider-responses", turn_id, "--database", str(database)]
    225 +    )
    226 +    assert inspected.exit_code == context.exit_code == responses.exit_code == 0
    227 +    assert '"stop_reason": "provider_done"' in inspected.stdout

• Ran .venv/bin/ruff check src tests
  └ All checks passed!

• Ran .venv/bin/mypy src
  └ Success: no issues found in 54 source files

• Ran .venv/bin/pytest -q
  └ ....................................................                     [100%]
    52 passed in 33.91s

• Updated Plan
  └ ✔ 核查模型、上下文、执行器与测试夹具
    ✔ 设计并迁移 autonomous audit 数据模型
    ✔ 实现 autonomous context、tool loop 与 world step
    ✔ 增加 CLI 检查与 scripted run
    □ 补充测试并运行完整质量检查

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 完整回归现已通过，共 52 passed；ruff 和 mypy 也全部通过。当前实现保留了旧的确定性 step/run，新增独立的 autonomous 路径，因此既能继续做机
  制验证，也能运行 ScriptedProvider 自主闭环。现在进行最后的覆盖率、CLI 实跑和变更审计。

• Ran git status --short && git diff --stat && git diff --check
  └  M src/emergence_world/agents/context.py
     M src/emergence_world/agents/models.py
    … +21 lines (ctrl + t to view transcript)
     tests/test_world.py                   |  30 ++++++
     9 files changed, 401 insertions(+), 14 deletions(-)

• Ran rm -f /tmp/autonomous-cli-audit.db; PYTHONPATH=src .venv/bin/python -m emergence_world.cli init --database /tmp/
  │ autonomous-cli-audit.db; PYTHONPATH=src .venv/bin/python -m emergence_world.cli run-autonomous --turns 3 --provider scripted
  │ --database /tmp/autonomous-cli-audit.db; PYTHONPATH=src .venv/bin/python -m emergence_world.cli replay --database /tmp/
  │ … +1 lines
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +54 lines (ctrl + t to view transcript)
      }
    }

■ Conversation interrupted - tell the model what to do differently. Something went wrong? Hit `/feedback` to report the issue.


› 继续工作


■ You've hit your usage limit. Upgrade to Pro (https://chatgpt.com/explore/pro), visit https://chatgpt.com/codex/settings/usage to
purchase more credits or try again at 3:24 PM.


› 保存完整的记录到codex-instruct-history


■ You've hit your usage limit. Upgrade to Pro (https://chatgpt.com/explore/pro), visit https://chatgpt.com/codex/settings/usage to
purchase more credits or try again at 3:24 PM.


› Implement {feature}

  gpt-5.5 default · /workspaces/codespaces-blank