    404 +            "--output-cost-per-million-tokens-usd is required for the openai provider"
    405 +        )
    406 +    return OpenAIProviderConfig(
    407 +        model=model,
    408 +        smoke_config=smoke_config,
    409 +        input_cost_per_million_tokens_usd=input_cost_per_million_tokens_usd,
    410 +        output_cost_per_million_tokens_usd=output_cost_per_million_tokens_usd,
    411 +    )
    412 +
    413 +
    414  def experiment_run_view(run: ExperimentRun) -> dict[str, Any]:

• Edited Emergence-World/src/emergence_world/cli.py (+77 -8)
    460      ),
    461 +    model: str | None = typer.Option(None, help="Provider model identifier."),
    462 +    input_cost_per_million_tokens_usd: float | None = typer.Option(
    463 +        None, min=0, help="Explicit input token price used for budget enforcement."
    464 +    ),
    465 +    output_cost_per_million_tokens_usd: float | None = typer.Option(
    466 +        None, min=0, help="Explicit output token price used for budget enforcement."
    467 +    ),
    468  ) -> None:
        ⋮
    470
    464 -    if provider != "scripted":
    465 -        raise typer.BadParameter("only the scripted provider is currently supported")
    471 +    if provider not in {"scripted", "openai"}:
    472 +        raise typer.BadParameter("provider must be scripted or openai")
    473      if mode not in {"development", "formal"}:
        ⋮
    476      session_factory = session_factory_for(database)
    470 -    provider_name, provider_model, provider_parameters = scripted_provider_metadata()
    477 +    smoke_config = ProviderSmokeConfig(max_turns=turns)
    478 +    if provider == "scripted":
    479 +        provider_name, provider_model, provider_parameters = scripted_provider_metadata(
    480 +            smoke_config
    481 +        )
    482 +    else:
    483 +        config = openai_provider_config(
    484 +            model=model,
    485 +            smoke_config=smoke_config,
    486 +            input_cost_per_million_tokens_usd=input_cost_per_million_tokens_usd,
    487 +            output_cost_per_million_tokens_usd=output_cost_per_million_tokens_usd,
    488 +        )
    489 +        provider_name, provider_model, provider_parameters = (
    490 +            OpenAIProvider.provider_name,
    491 +            config.model,
    492 +            OpenAIProvider(config).manifest_parameters(),
    493 +        )
    494      try:
        ⋮
    535      world_id: str,
    513 -    provider: ScriptedProvider,
    536 +    provider: AgentProvider,
    537      minutes: int,
        ⋮
    562      ),
    563 +    model: str | None = typer.Option(None, help="Provider model identifier."),
    564 +    allow_external_provider: bool = typer.Option(
    565 +        False,
    566 +        "--allow-external-provider",
    567 +        help="Explicitly permit requests to a real provider.",
    568 +    ),
    569 +    max_provider_calls_per_turn: int = typer.Option(2, min=1),
    570 +    max_tool_calls_per_turn: int = typer.Option(1, min=1),
    571 +    max_input_tokens_per_request: int = typer.Option(32_000, min=1),
    572 +    max_output_tokens_per_request: int = typer.Option(1_000, min=1),
    573 +    max_total_cost_usd: float = typer.Option(0.25, min=0),
    574 +    timeout_seconds: float = typer.Option(60.0, min=0.1),
    575 +    max_retries: int = typer.Option(1, min=0),
    576 +    input_cost_per_million_tokens_usd: float | None = typer.Option(None, min=0),
    577 +    output_cost_per_million_tokens_usd: float | None = typer.Option(None, min=0),
    578  ) -> None:
        ⋮
    580
    543 -    if provider != "scripted":
    544 -        raise typer.BadParameter("only the scripted provider is currently supported")
    581 +    if provider not in {"scripted", "openai"}:
    582 +        raise typer.BadParameter("provider must be scripted or openai")
    583 +    if provider == "openai" and not allow_external_provider:
    584 +        raise typer.BadParameter(
    585 +            "openai provider requires explicit --allow-external-provider"
    586 +        )
    587      if mode not in {"development", "formal"}:
        ⋮
    590      session_factory = session_factory_for(database)
    549 -    provider_name, provider_model, provider_parameters = scripted_provider_metadata()
    591 +    smoke_config = provider_smoke_config(
    592 +        turns=turns,
    593 +        max_provider_calls_per_turn=max_provider_calls_per_turn,
    594 +        max_tool_calls_per_turn=max_tool_calls_per_turn,
    595 +        max_input_tokens_per_request=max_input_tokens_per_request,
    596 +        max_output_tokens_per_request=max_output_tokens_per_request,
    597 +        max_total_cost_usd=max_total_cost_usd,
    598 +        timeout_seconds=timeout_seconds,
    599 +        max_retries=max_retries,
    600 +    )
    601 +    real_provider: OpenAIProvider | None = None
    602 +    if provider == "scripted":
    603 +        provider_name, provider_model, provider_parameters = scripted_provider_metadata(
    604 +            smoke_config
    605 +        )
    606 +    else:
    607 +        config = openai_provider_config(
    608 +            model=model,
    609 +            smoke_config=smoke_config,
    610 +            input_cost_per_million_tokens_usd=input_cost_per_million_tokens_usd,
    611 +            output_cost_per_million_tokens_usd=output_cost_per_million_tokens_usd,
    612 +        )
    613 +        real_provider = OpenAIProvider(config)
    614 +        provider_name, provider_model, provider_parameters = (
    615 +            real_provider.provider_name,
    616 +            real_provider.model_name,
    617 +            real_provider.manifest_parameters(),
    618 +        )
    619      effective_run_id = run_id or f"autonomous-{uuid4()}"
        ⋮
    655                          world_id,
    587 -                        scripted_turn_provider(sequence),
    656 +                        real_provider or scripted_turn_provider(sequence),
    657                          minutes,

• Edited 2 files (+8 -5)
  └ Emergence-World/src/emergence_world/agents/providers/openai.py (+7 -4)
    36
    37 +    def manifest_parameters(self) -> dict[str, Any]:
    38 +        return {
    39 +            "api": "responses",
    40 +            **self.model_dump(mode="json", exclude={"model"}),
    41 +        }
    42 +
    43
       ⋮
    79      def manifest_parameters(self) -> dict[str, Any]:
    74 -        return {
    75 -            "api": "responses",
    76 -            **self.config.model_dump(mode="json", exclude={"model"}),
    77 -        }
    80 +        return self.config.manifest_parameters()
    81

  └ Emergence-World/src/emergence_world/cli.py (+1 -1)
    491              config.model,
    492 -            OpenAIProvider(config).manifest_parameters(),
    492 +            config.manifest_parameters(),
    493          )

• Edited Emergence-World/src/emergence_world/agents/providers/openai.py (+14 -10)
    175              "instructions": load_agent_turn_prompt(),
    176 -            "input": {
    177 -                "context": context.model_dump(mode="json"),
    178 -                "prior_tool_results": [
    179 -                    result.model_dump(mode="json") for result in prior_results
    180 -                ],
    181 -                "remaining_tool_call_budget": min(
    182 -                    tool_call_budget, self.smoke_config.max_tool_calls_per_turn
    183 -                ),
    184 -            },
    176 +            "input": json.dumps(
    177 +                {
    178 +                    "context": context.model_dump(mode="json"),
    179 +                    "prior_tool_results": [
    180 +                        result.model_dump(mode="json") for result in prior_results
    181 +                    ],
    182 +                    "remaining_tool_call_budget": min(
    183 +                        tool_call_budget, self.smoke_config.max_tool_calls_per_turn
    184 +                    ),
    185 +                },
    186 +                ensure_ascii=True,
    187 +                separators=(",", ":"),
    188 +                sort_keys=True,
    189 +            ),
    190              "tools": [
        ⋮
    195                      "parameters": tool.argument_schema,
    191 -                    "strict": True,
    196                  }

• Ran .venv/bin/ruff check src/emergence_world/agents/providers/openai.py src/emergence_world/cli.py
  └ All checks passed!

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli run-autonomous --help | sed -n '1,240p'
  └
     Usage: python -m emergence_world.cli run-autonomous [OPTIONS]
    … +43 lines (ctrl + t to view transcript)
    ╰──────────────────────────────────────────────────────────────────────────────╯


• Ran .venv/bin/mypy src/emergence_world/agents/providers/openai.py src/emergence_world/cli.py
  └ Success: no issues found in 2 source files

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• CLI 已接入显式保护开关和最小预算参数，默认仍是完全离线 scripted provider。接下来补充真实适配器的内存 mock 验收：请求结构、函数调用解析、
  成本预算、失败分类、API key 不进入请求/Manifest/审计，以及 CLI 未显式授权时绝不联网。

• Edited 2 files (+154 -1)
  └ Emergence-World/src/emergence_world/cli.py (+1 -1)
    578  ) -> None:
    579 -    """Run audited autonomous turns using the deterministic scripted provider."""
    579 +    """Run audited autonomous turns using an explicitly selected provider."""
    580

  └ Emergence-World/tests/test_agents.py (+153 -0)
     24  from emergence_world.agents.providers.manual import ManualProvider
     25 +from emergence_world.agents.providers.openai import OpenAIProvider, OpenAIProviderConfig
     26  from emergence_world.agents.providers.recording import (
        ⋮
     32  from emergence_world.agents.providers.smoke import ProviderFailure, ProviderFailureCode
     33 +from emergence_world.agents.providers.smoke import ProviderSmokeConfig
     34  from emergence_world.agents.runtime import AgentTurnRuntime
        ⋮
    300      assert provider.last_audit_metadata.parse_error == f"{code.value}: offline failure"
    301 +
    302 +
    303 +class FakeResponse:
    304 +    def __init__(self, payload: dict[str, object]) -> None:
    305 +        self.payload = payload
    306 +
    307 +    def model_dump(self, *, mode: str) -> dict[str, object]:
    308 +        assert mode == "json"
    309 +        return self.payload
    310 +
    311 +
    312 +class FakeResponsesAPI:
    313 +    def __init__(
    314 +        self, response: FakeResponse | None = None, failure: Exception | None = None
    315 +    ) -> None:
    316 +        self.response = response
    317 +        self.failure = failure
    318 +        self.requests: list[dict[str, object]] = []
    319 +
    320 +    async def create(self, **kwargs: object) -> object:
    321 +        self.requests.append(kwargs)
    322 +        if self.failure is not None:
    323 +            raise self.failure
    324 +        assert self.response is not None
    325 +        return self.response
    326 +
    327 +
    328 +class FakeOpenAIClient:
    329 +    def __init__(self, responses: FakeResponsesAPI) -> None:
    330 +        self.responses = responses
    331 +
    332 +
    333 +def openai_config(**updates: object) -> OpenAIProviderConfig:
    334 +    config: dict[str, object] = {
    335 +        "model": "mock-model",
    336 +        "smoke_config": ProviderSmokeConfig(
    337 +            max_turns=1,
    338 +            max_provider_calls_per_turn=2,
    339 +            max_tool_calls_per_turn=1,
    340 +            max_input_tokens_per_request=1_000,
    341 +            max_output_tokens_per_request=100,
    342 +            max_total_cost_usd=0.01,
    343 +            timeout_seconds=1,
    344 +            max_retries=0,
    345 +        ),
    346 +        "input_cost_per_million_tokens_usd": 1.0,
    347 +        "output_cost_per_million_tokens_usd": 2.0,
    348 +    }
    349 +    config.update(updates)
    350 +    return OpenAIProviderConfig.model_validate(config)
    351 +
    352 +
    353 +def test_openai_provider_builds_structured_request_and_parses_function_call() -> None:
    354 +    _, context = make_context()
    355 +    responses = FakeResponsesAPI(
    356 +        FakeResponse(
    357 +            {
    358 +                "output": [
    359 +                    {
    360 +                        "type": "function_call",
    361 +                        "call_id": "call-1",
    362 +                        "name": "list_proposals",
    363 +                        "arguments": "{}",
    364 +                    }
    365 +                ],
    366 +                "usage": {
    367 +                    "input_tokens": 100,
    368 +                    "output_tokens": 20,
    369 +                    "total_tokens": 120,
    370 +                },
    371 +            }
    372 +        )
    373 +    )
    374 +    provider = OpenAIProvider(openai_config(), client=FakeOpenAIClient(responses))
    375 +
    376 +    decision = asyncio.run(provider.decide(context, 1))
    377 +
    378 +    assert decision.tool_calls[0].tool_name == "list_proposals"
    379 +    assert responses.requests[0]["model"] == "mock-model"
    380 +    assert responses.requests[0]["parallel_tool_calls"] is False
    381 +    assert "API key" not in str(responses.requests[0])
    382 +    assert provider.last_audit_metadata is not None
    383 +    assert provider.last_audit_metadata.total_tokens == 120
    384 +    assert provider.last_audit_metadata.cost_usd == pytest.approx(0.00014)
    385 +
    386 +
    387 +def test_openai_provider_natural_language_cannot_change_state() -> None:
    388 +    _, context = make_context()
    389 +    responses = FakeResponsesAPI(
    390 +        FakeResponse(
    391 +            {
    392 +                "output": [
    393 +                    {
    394 +                        "type": "message",
    395 +                        "content": [
    396 +                            {
    397 +                                "type": "output_text",
    398 +                                "text": "I moved to Town Hall.",
    399 +                            }
    403 +                "usage": {},
    404 +            }
    405 +        )
    406 +    )
    407 +    provider = OpenAIProvider(openai_config(), client=FakeOpenAIClient(responses))
    408 +
    409 +    decision = asyncio.run(provider.decide(context, 1))
    410 +
    411 +    assert decision.tool_calls == ()
    412 +    assert decision.reasoning_text == "I moved to Town Hall."
    413 +    assert decision.terminate is True
    414 +
    415 +
    416 +def test_openai_provider_enforces_cost_budget_and_classifies_client_failure() -> None:
    417 +    _, context = make_context()
    418 +    costly = FakeResponsesAPI(
    419 +        FakeResponse(
    420 +            {
    421 +                "output": [],
    422 +                "usage": {"input_tokens": 1_000, "output_tokens": 100},
    423 +            }
    424 +        )
    425 +    )
    426 +    provider = OpenAIProvider(
    427 +        openai_config(
    428 +            smoke_config=ProviderSmokeConfig(
    429 +                max_turns=1,
    430 +                max_provider_calls_per_turn=1,
    431 +                max_tool_calls_per_turn=1,
    432 +                max_input_tokens_per_request=2_000,
    433 +                max_output_tokens_per_request=200,
    434 +                max_total_cost_usd=0.0001,
    435 +                timeout_seconds=1,
    436 +                max_retries=0,
    437 +            )
    438 +        ),
    439 +        client=FakeOpenAIClient(costly),
    440 +    )
    441 +    with pytest.raises(ProviderFailure) as captured:
    442 +        asyncio.run(provider.decide(context, 1))
    443 +    assert captured.value.code == ProviderFailureCode.BUDGET_EXCEEDED
    444 +
    445 +    failed = OpenAIProvider(
    446 +        openai_config(),
    447 +        client=FakeOpenAIClient(FakeResponsesAPI(failure=RuntimeError("offline"))),
    448 +    )
    449 +    with pytest.raises(ProviderFailure) as captured:
    450 +        asyncio.run(failed.decide(context, 1))
    451 +    assert captured.value.code == ProviderFailureCode.PROVIDER_ERROR

• Explored
  └ Read test_manifest.py

• Edited Emergence-World/tests/test_manifest.py (+66 -0)
    150
    151 +def test_openai_run_manifest_is_created_without_api_key_or_request(
    152 +    tmp_path: Path, monkeypatch
    153 +) -> None:
    154 +    secret = "openai-secret-must-not-persist"
    155 +    monkeypatch.setenv("OPENAI_API_KEY", secret)
    156 +    database, session_factory, _ = initialized_database(tmp_path)
    157 +    result = CliRunner().invoke(
    158 +        app,
    159 +        [
    160 +            "create-run",
    161 +            "--run-id",
    162 +            "openai-manifest",
    163 +            "--provider",
    164 +            "openai",
    165 +            "--model",
    166 +            "mock-model",
    167 +            "--turns",
    168 +            "1",
    169 +            "--input-cost-per-million-tokens-usd",
    170 +            "1",
    171 +            "--output-cost-per-million-tokens-usd",
    172 +            "2",
    173 +            "--database",
    174 +            str(database),
    175 +        ],
    176 +    )
    177 +
    178 +    assert result.exit_code == 0
    179 +    with session_factory() as session:
    180 +        run = session.scalar(
    181 +            select(ExperimentRun).where(ExperimentRun.run_id == "openai-manifest")
    182 +        )
    183 +        assert run is not None
    184 +        assert run.provider_name == "openai"
    185 +        assert run.provider_model == "mock-model"
    186 +        assert run.provider_parameters_json["api"] == "responses"
    187 +        assert run.provider_parameters_json["smoke_config"]["max_turns"] == 1
    188 +        assert secret not in str(run.provider_parameters_json)
    189 +
    190 +
    191 +def test_openai_run_requires_explicit_external_provider_permission(tmp_path: Path) -> None:
    192 +    database, _, _ = initialized_database(tmp_path)
    193 +
    194 +    result = CliRunner().invoke(
    195 +        app,
    196 +        [
    197 +            "run-autonomous",
    198 +            "--provider",
    199 +            "openai",
    200 +            "--model",
    201 +            "mock-model",
    202 +            "--turns",
    203 +            "1",
    204 +            "--input-cost-per-million-tokens-usd",
    205 +            "1",
    206 +            "--output-cost-per-million-tokens-usd",
    207 +            "2",
    208 +            "--database",
    209 +            str(database),
    210 +        ],
    211 +    )
    212 +
    213 +    assert result.exit_code != 0
    214 +    assert "requires explicit --allow-external-provider" in result.output
    215 +
    216 +
    217  def test_run_autonomous_creates_and_completes_manifest(tmp_path: Path) -> None:

• Ran .venv/bin/ruff check src tests
  └ All checks passed!

• Ran .venv/bin/mypy src
  └ Success: no issues found in 64 source files

• Ran .venv/bin/pytest tests/test_agents.py tests/test_manifest.py tests/test_autonomous.py -q
  └ ................F...................                                     [100%]
    =================================== FAILURES ===================================
    … +41 lines (ctrl + t to view transcript)
    FAILED tests/test_agents.py::test_openai_provider_builds_structured_request_and_parses_function_call
    1 failed, 35 passed in 18.83s

• Edited Emergence-World/tests/test_agents.py (+6 -2)
    352
    353 -def test_openai_provider_builds_structured_request_and_parses_function_call() -> None:
    353 +def test_openai_provider_builds_structured_request_and_parses_function_call(
    354 +    monkeypatch,
    355 +) -> None:
    356 +    secret = "openai-request-secret"
    357 +    monkeypatch.setenv("OPENAI_API_KEY", secret)
    358      _, context = make_context()
        ⋮
    384      assert responses.requests[0]["parallel_tool_calls"] is False
    381 -    assert "API key" not in str(responses.requests[0])
    385 +    assert secret not in str(responses.requests[0])
    386      assert provider.last_audit_metadata is not None

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 专项测试暴露的是测试断言误把 Prompt 中的“API key”安全说明当成泄漏，实际请求中没有密钥值；已改为验证真实环境变量值不出现。现在补一条端到
  端 mock 审计测试，确保 OpenAI 响应的 raw payload、token 与 cost 能进入 provider_interactions，同时密钥仍不落库。

• Edited Emergence-World/tests/test_autonomous.py (+59 -0)
     18  )
     19 +from emergence_world.agents.providers.openai import OpenAIProvider, OpenAIProviderConfig
     20  from emergence_world.agents.providers.smoke import ProviderFailure, ProviderFailureCode
     21 +from emergence_world.agents.providers.smoke import ProviderSmokeConfig
     22  from emergence_world.db.models import (
        ⋮
    275          assert secret not in str(audit.parse_error)
    276 +
    277 +
    278 +def test_openai_provider_mock_response_is_fully_audited(
    279 +    tmp_path: Path, monkeypatch
    280 +) -> None:
    281 +    secret = "openai-audit-secret"
    282 +    monkeypatch.setenv("OPENAI_API_KEY", secret)
    283 +    session_factory, world_id = initialized_world(tmp_path)
    284 +
    285 +    class Response:
    286 +        def model_dump(self, *, mode: str):
    287 +            assert mode == "json"
    288 +            return {
    289 +                "id": "response-1",
    290 +                "output": [],
    291 +                "usage": {
    292 +                    "input_tokens": 100,
    293 +                    "output_tokens": 20,
    294 +                    "total_tokens": 120,
    295 +                },
    296 +            }
    297 +
    298 +    class Responses:
    299 +        async def create(self, **kwargs):
    300 +            assert secret not in str(kwargs)
    301 +            return Response()
    302 +
    303 +    class Client:
    304 +        responses = Responses()
    305 +
    306 +    provider = OpenAIProvider(
    307 +        OpenAIProviderConfig(
    308 +            model="mock-model",
    309 +            smoke_config=ProviderSmokeConfig(max_turns=1),
    310 +            input_cost_per_million_tokens_usd=1,
    311 +            output_cost_per_million_tokens_usd=2,
    312 +        ),
    313 +        client=Client(),
    314 +    )
    315 +
    316 +    async def scenario():
    317 +        with sync_transaction(session_factory) as session:
    318 +            return await autonomous_step_world(session, world_id, provider)
    319 +
    320 +    result = asyncio.run(scenario())
    321 +
    322 +    with session_factory() as session:
    323 +        audit = session.scalar(
    324 +            select(ProviderInteraction).where(ProviderInteraction.turn_id == result.turn_id)
    325 +        )
    326 +        assert audit is not None
    327 +        assert audit.provider == "openai"
    328 +        assert audit.raw_response_json["id"] == "response-1"
    329 +        assert audit.total_tokens == 120
    330 +        assert audit.cost_usd == pytest.approx(0.00014)
    331 +        assert secret not in str(audit.request_json)
    332 +        assert secret not in str(audit.raw_response_json)

  └ Success: no issues found in 64 source files

• Ran .venv/bin/pytest tests/test_agents.py tests/test_manifest.py tests/test_autonomous.py -q
  └ .....................................                                    [100%]
    37 passed in 17.13s

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 端到端 mock 审计已通过。继续复核时发现一个真实运行必须修正的问题：CLI 的 Provider 失败会让当前事务整体回滚，从而丢失刚写入的失败审计。接
  下来调整单轮事务边界，使 Provider 失败时保存失败 Turn 和 provider_interactions 后再向上抛出，世界状态仍不推进。

• Edited Emergence-World/src/emergence_world/cli.py (+25 -3)
     26      ProviderInteraction,
     27 +    SimulationClock,
     28      ToolCall,
        ⋮
     49  from emergence_world.agents.providers.scripted import ScriptedProvider
     49 -from emergence_world.agents.providers.smoke import ProviderSmokeConfig
     50 +from emergence_world.agents.providers.smoke import ProviderFailure, ProviderSmokeConfig
     51  from emergence_world.tools import ManualToolExecutor
        ⋮
    539  ) -> Any:
    539 -    with sync_transaction(session_factory) as session:
    540 -        return await autonomous_step_world(session, world_id, provider, minutes)
    540 +    session = session_factory()
    541 +    try:
    542 +        result = await autonomous_step_world(session, world_id, provider, minutes)
    543 +        session.commit()
    544 +        return result
    545 +    except ProviderFailure as failure:
    546 +        turn = session.scalar(
    547 +            select(Turn)
    548 +            .where(Turn.world_id == world_id, Turn.status == TurnStatus.RUNNING)
    549 +            .order_by(Turn.sequence_number.desc())
    550 +        )
    551 +        clock = session.get(SimulationClock, world_id)
    552 +        if turn is not None:
    553 +            turn.status = TurnStatus.FAILED
    554 +            turn.stop_reason = f"provider_failure:{failure.code.value}"
    555 +            turn.ended_at = clock.current_time if clock is not None else utc_now()
    556 +        session.commit()
    557 +        raise
    558 +    except Exception:
    559 +        session.rollback()
    560 +        raise
    561 +    finally:
    562 +        session.close()
    563

• Explored
  └ Search from emergence_world.db.types in cli.py
    Search class TurnStatus in types.py

• Edited Emergence-World/src/emergence_world/cli.py (+1 -1)
    40  from emergence_world.db.base import utc_now
    41 -from emergence_world.db.types import ExperimentRunStatus
    41 +from emergence_world.db.types import ExperimentRunStatus, TurnStatus
    42  from emergence_world.experiments import create_experiment_run

• Edited Emergence-World/tests/test_autonomous.py (+20 -0)
     21  from emergence_world.agents.providers.smoke import ProviderSmokeConfig
     22 +from emergence_world.cli import run_one_autonomous_turn
     23  from emergence_world.db.models import (
        ⋮
    278
    279 +def test_cli_turn_boundary_persists_provider_failure_audit(tmp_path: Path) -> None:
    280 +    session_factory, world_id = initialized_world(tmp_path)
    281 +    provider = RecordingProvider(
    282 +        [RecordedProviderFailure(ProviderFailureCode.TIMEOUT, "offline timeout")]
    283 +    )
    284 +
    285 +    with pytest.raises(ProviderFailure):
    286 +        asyncio.run(run_one_autonomous_turn(session_factory, world_id, provider, 30))
    287 +
    288 +    with session_factory() as session:
    289 +        turn = session.scalar(select(Turn))
    290 +        audit = session.scalar(select(ProviderInteraction))
    291 +        assert turn is not None
    292 +        assert turn.status == TurnStatus.FAILED
    293 +        assert turn.stop_reason == "provider_failure:timeout"
    294 +        assert audit is not None
    295 +        assert audit.parse_error == "timeout: offline timeout"
    296 +
    297 +
    298  def test_openai_provider_mock_response_is_fully_audited(

• Ran .venv/bin/ruff check src tests
  └ F821 Undefined name `TurnStatus`
       --> tests/test_autonomous.py:292:31
    … +8 lines (ctrl + t to view transcript)

    Found 1 error.

• Ran .venv/bin/mypy src
  └ Success: no issues found in 64 source files

• Ran .venv/bin/pytest tests/test_agents.py tests/test_manifest.py tests/test_autonomous.py -q
  └ ....................................F.                                   [100%]
    =================================== FAILURES ===================================
    … +33 lines (ctrl + t to view transcript)
    FAILED tests/test_autonomous.py::test_cli_turn_boundary_persists_provider_failure_audit
    1 failed, 37 passed in 20.49s

• Edited Emergence-World/tests/test_autonomous.py (+1 -1)
    37  )
    38 -from emergence_world.db.types import AgentStatus, ToolCallStatus
    38 +from emergence_world.db.types import AgentStatus, ToolCallStatus, TurnStatus
    39  from emergence_world.seed import import_seed_bundle, load_seed_bundle

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 失败审计事务边界已修复，专项失败仅是测试缺少枚举导入。另一个边界也需要收紧：Responses API 返回空 output 不能被当作正常终止，必须分类为
  empty_response；我会同步调整 mock 测试，让正常无工具动作通过实际 message 响应表达。

• Edited 3 files (+16 -2)
  └ Emergence-World/src/emergence_world/agents/providers/openai.py (+4 -0)
    250                      reasoning.append(content["text"])
    251 +    if not calls and not reasoning:
    252 +        raise ProviderFailure(
    253 +            ProviderFailureCode.EMPTY_RESPONSE, "provider returned no calls or message"
    254 +        )
    255      return {

  └ Emergence-World/tests/test_agents.py (+6 -1)
    424              {
    425 -                "output": [],
    425 +                "output": [
    426 +                    {
    427 +                        "type": "message",
    428 +                        "content": [{"type": "output_text", "text": "Done."}],
    429 +                    }
    430 +                ],
    431                  "usage": {"input_tokens": 1_000, "output_tokens": 100},

  └ Emergence-World/tests/test_autonomous.py (+6 -1)
    309                  "id": "response-1",
    310 -                "output": [],
    310 +                "output": [
    311 +                    {
    312 +                        "type": "message",
    313 +                        "content": [{"type": "output_text", "text": "Done."}],
    314 +                    }
    315 +                ],
    316                  "usage": {

• Ran .venv/bin/ruff check src tests
  └ All checks passed!

• Ran .venv/bin/mypy src
  └ Success: no issues found in 64 source files

• Ran .venv/bin/pytest -q
  └ ........................................................................ [ 91%]
    .......                                                                  [100%]
    79 passed in 48.74s

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 完整回归现已通过：79 tests passed，ruff 与 mypy 均通过。最后执行无网络的预演：全新数据库 migration/seed、scripted 单轮、创建 OpenAI
  smoke Manifest、验证未加授权开关的 OpenAI run 被拒绝，再运行 readiness-check。

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli init --database /tmp/emergence-openai-preflight-20260615.db --random-seed 42
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +7 lines (ctrl + t to view transcript)
    World created: 10be473e-0220-4a21-a1de-65ed85a5713b (10 agents, 35 landmarks, 38
    tools, 5 articles)

⚠ Heads up, you have less than 10% of your 5h limit left. Run /status for a breakdown.

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli run-autonomous --database /tmp/emergence-openai-preflight-20260615.db
  │ --provider scripted --turns 1 --random-seed 42 --run-id preflight-scripted
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +7 lines (ctrl + t to view transcript)
      "state_hash": "4e602da9154f711131913eb3130726aaee6f58881ebd0ab8b73f72a0c059c850"
    }

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli create-run --database /tmp/emergence-openai-preflight-20260615.db --run-id
  │ preflight-openai --provider openai --model mock-model --turns 1 --random-seed 42 --input-cost-per-million-tokens-usd 1
  │ --output-cost-per-million-tokens-usd 2
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +73 lines (ctrl + t to view transcript)
      "completed_at": null
    }

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 预演发现 create-run --provider openai 虽然不联网且 Manifest 正确，但仍继承了通用的宽松预算（每轮 8 次 Provider、30 次工具调用）。这不符
  合“最小验证”目标，我会将 OpenAI Manifest 默认预算收紧到与真实运行 CLI 相同的 2 次 Provider、1 次工具调用、较低输出和成本上限。

• Edited 2 files (+10 -1)
  └ Emergence-World/src/emergence_world/cli.py (+9 -1)
    477      session_factory = session_factory_for(database)
    478 -    smoke_config = ProviderSmokeConfig(max_turns=turns)
    478      if provider == "scripted":
    479 +        smoke_config = ProviderSmokeConfig(max_turns=turns)
    480          provider_name, provider_model, provider_parameters = scripted_provider_metadata(
        ⋮
    483      else:
    484 +        smoke_config = ProviderSmokeConfig(
    485 +            max_turns=turns,
    486 +            max_provider_calls_per_turn=2,
    487 +            max_tool_calls_per_turn=1,
    488 +            max_output_tokens_per_request=1_000,
    489 +            max_total_cost_usd=0.25,
    490 +            max_retries=1,
    491 +        )
    492          config = openai_provider_config(

  └ Emergence-World/tests/test_manifest.py (+1 -0)
    187          assert run.provider_parameters_json["smoke_config"]["max_turns"] == 1
    188 +        assert run.provider_parameters_json["smoke_config"]["max_tool_calls_per_turn"] == 1
    189          assert secret not in str(run.provider_parameters_json)

• Ran .venv/bin/ruff check src tests
  └ All checks passed!

• Ran .venv/bin/mypy src
  └ Success: no issues found in 64 source files

• Ran .venv/bin/pytest -q
  └ ........................................................................ [ 91%]
    .......                                                                  [100%]
    79 passed in 50.36s

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli run-autonomous --database /tmp/emergence-openai-preflight-20260615.db
  │ --provider openai --model mock-model --turns 1 --input-cost-per-million-tokens-usd 1 --output-cost-per-million-tokens-usd 2
  └ Usage: python -m emergence_world.cli run-autonomous [OPTIONS]
    Try 'python -m emergence_world.cli run-autonomous --help' for help.
    ╭─ Error ──────────────────────────────────────────────────────────────────────╮
    │ Invalid value: openai provider requires explicit --allow-external-provider   │
    ╰──────────────────────────────────────────────────────────────────────────────╯

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli create-run --database /tmp/emergence-openai-preflight-20260615.db --run-id
  │ preflight-openai-minimal --provider openai --model mock-model --turns 1 --random-seed 42 --input-cost-per-million-tokens-usd 1
  │ --output-cost-per-million-tokens-usd 2
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +73 lines (ctrl + t to view transcript)
      "completed_at": null
    }

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli readiness-check --database /tmp/emergence-openai-preflight-20260615.db
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +54 lines (ctrl + t to view transcript)
      }
    }

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 离线预演全部通过，且未授权的 OpenAI 命令在建立客户端或请求前被拒绝。当前 readiness 唯一失败项仍是 git_dirty=true；因为你要求完成上一步中
  的“提交当前代码并确保干净”，我现在检查最终差异与生成物，然后提交本轮完整 readiness/provider 变更。

• Ran git diff --check && git diff --stat
  └  src/emergence_world/agents/audit.py              |  88 ++++-
     src/emergence_world/agents/providers/__init__.py |  20 +-
    … +6 lines (ctrl + t to view transcript)
     tests/test_db.py                                 |   2 +
     9 files changed, 1003 insertions(+), 41 deletions(-)

• Ran git status --short -- .coverage htmlcov coverage.xml .pytest_cache .mypy_cache .ruff_cache && git status --short
  └  M src/emergence_world/agents/audit.py
     M src/emergence_world/agents/providers/__init__.py
    … +15 lines (ctrl + t to view transcript)
    ?? src/emergence_world/experiments/
    ?? tests/test_manifest.py

• Ran git log -1 --oneline
  └ 8df74ba finish agent loop

• Ran git add src tests prompts && git commit -m "add provider smoke readiness gate"
  └ [main 4387fc0] add provider smoke readiness gate
     21 files changed, 2662 insertions(+), 41 deletions(-)
    … +10 lines (ctrl + t to view transcript)
     create mode 100644 src/emergence_world/experiments/readiness.py
     create mode 100644 tests/test_manifest.py

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 提交已完成，commit 为 4387fc0。现在用提交后的干净工作树重新建立全新数据库，并以 formal 模式创建最小 OpenAI Manifest；随后运行最终
  readiness-check，确认 ready_for_real_provider 能真正变为 true。

• Ran git log -1 --oneline
  └ 4387fc0 add provider smoke readiness gate

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli init --database /tmp/emergence-openai-ready-clean-20260615.db --random-seed
  │ 42
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +7 lines (ctrl + t to view transcript)
    World created: 049f93a2-050b-4eb7-8bda-42d92b976570 (10 agents, 35 landmarks, 38
    tools, 5 articles)

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli run-autonomous --database /tmp/emergence-openai-ready-clean-20260615.db
  │ --provider scripted --turns 1 --random-seed 42 --run-id clean-scripted-audit --mode formal
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +7 lines (ctrl + t to view transcript)
      "state_hash": "4e602da9154f711131913eb3130726aaee6f58881ebd0ab8b73f72a0c059c850"
    }

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli create-run --database /tmp/emergence-openai-ready-clean-20260615.db --run-id
  │ clean-openai-smoke --provider openai --model mock-model --turns 1 --random-seed 42 --input-cost-per-million-tokens-usd 1
  │ --output-cost-per-million-tokens-usd 2 --mode formal
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +51 lines (ctrl + t to view transcript)
      "completed_at": null
    }

⚠ Heads up, you have less than 5% of your 5h limit left. Run /status for a breakdown.

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli readiness-check --database /tmp/emergence-openai-ready-clean-20260615.db
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +32 lines (ctrl + t to view transcript)
      }
    }

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• Ran .venv/bin/pytest -q
  └ ..................................F......F.............................. [ 91%]
    .......                                                                  [100%]
    … +102 lines (ctrl + t to view transcript)
    2 failed, 77 passed in 48.60s

• Explored
  └ Read test_manifest.py

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 失败原因已定位为两条依赖仓库当前状态的非确定性测试。修复方式是不再假设运行测试时仓库一定 dirty，并在 formal-mode 测试中显式注入 dirty
  git snapshot，使 clean 与 dirty 环境下结果一致。

• Edited Emergence-World/tests/test_manifest.py (+16 -2)
     78          assert run.provider_parameters_json["smoke_config"]["max_retries"] == 2
     79 -        assert run.environment_json["git_dirty"] is True
     79 +        assert isinstance(run.environment_json["git_dirty"], bool)
     80          assert isinstance(run.environment_json["dirty_files"], list)
        ⋮
    276  def test_formal_run_rejects_dirty_git_and_readiness_reports_gate(
    277 -    tmp_path: Path,
    277 +    tmp_path: Path, monkeypatch
    278  ) -> None:
        ⋮
    280      runner = CliRunner()
    281 +    dirty_snapshot = {
    282 +        "git_dirty": True,
    283 +        "dirty_files": ["test-dirty-file"],
    284 +        "git_branch": "main",
    285 +        "git_commit": "test-commit",
    286 +    }
    287 +    monkeypatch.setattr(
    288 +        "emergence_world.experiments.manifest.git_snapshot",
    289 +        lambda root: dirty_snapshot,
    290 +    )
    291 +    monkeypatch.setattr(
    292 +        "emergence_world.experiments.readiness.git_snapshot",
    295

• Ran .venv/bin/ruff check src tests
  └ All checks passed!

• Ran .venv/bin/mypy src
  └ Success: no issues found in 64 source files

■ Conversation interrupted - tell the model what to do differently. Something went wrong? Hit `/feedback` to report the issue.


› 当前问题是什么


• 我先确认被中断的全量测试是否已经结束，以及当前 git 状态，避免把已修复的问题误报成仍存在。

• Ran git status --short && git log -2 --oneline
  └  M tests/test_manifest.py
    4387fc0 add provider smoke readiness gate
    8df74ba finish agent loop

• Waited for background terminal · .venv/bin/pytest -q

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 当前没有功能实现问题。

  刚才发现并已修复的问题是：两条测试错误地假设仓库运行时一定是 dirty，导致代码提交后：

  - pytest 出现 2 条失败
  - readiness-check 因 tests_status=failed，无法变为 ready

  目前状态：

  - 修复仅在 tests/test_manifest.py
  - ruff、mypy 已通过
  - 全量 pytest 正在运行，已到约 94%，尚未结束
  - 修复尚未提交，因此当前 git_dirty=true
  - 未调用任何真实模型/API

  待完成：等待 pytest 结束、提交测试修复，然后在干净工作树重新运行最终 readiness-check。

• Ran .venv/bin/pytest -q
  └ ........................................................................ [ 91%]
    .......                                                                  [100%]
    79 passed in 72.89s (0:01:12)

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────


› 继续运行


• 继续完成收尾：先等待全量 pytest 结束；通过后提交测试稳定性修复，再在干净工作树运行最终 readiness-check。

• 全量测试通过：79 passed。现在提交这条测试稳定性修复，随后用干净工作树重新创建正式 OpenAI smoke Manifest 并执行最终 gate。

• Ran git add tests/test_manifest.py && git commit -m "make readiness tests independent of worktree state"
  └ [main 2240426] make readiness tests independent of worktree state
     1 file changed, 16 insertions(+), 2 deletions(-)

• Ran git status --short
  └ (no output)

• Ran .venv/bin/ruff check src tests && .venv/bin/mypy src
  └ All checks passed!
    Success: no issues found in 64 source files

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 工作树已干净，ruff/mypy 通过。为了让 Manifest 记录最新 commit，我使用全新数据库重新执行 migration、seed、formal scripted 审计和 formal
  OpenAI smoke Manifest，然后运行完整 readiness-check。

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli init --database /tmp/emergence-openai-final-ready-20260615.db --random-seed
  │ 42
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +7 lines (ctrl + t to view transcript)
    World created: ef27f27a-2f48-4427-8211-7f83f0dbaace (10 agents, 35 landmarks, 38
    tools, 5 articles)

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli run-autonomous --database /tmp/emergence-openai-final-ready-20260615.db
  │ --provider scripted --turns 1 --random-seed 42 --run-id final-scripted-audit --mode formal
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +7 lines (ctrl + t to view transcript)
      "state_hash": "4e602da9154f711131913eb3130726aaee6f58881ebd0ab8b73f72a0c059c850"
    }

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli create-run --database /tmp/emergence-openai-final-ready-20260615.db --run-id
  │ final-openai-smoke --provider openai --model mock-model --turns 1 --random-seed 42 --input-cost-per-million-tokens-usd 1
  │ --output-cost-per-million-tokens-usd 2 --mode formal
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +51 lines (ctrl + t to view transcript)
      "completed_at": null
    }

• Ran PYTHONPATH=src .venv/bin/python -m emergence_world.cli readiness-check --database /tmp/emergence-openai-final-ready-20260615.db
  └ INFO  [alembic.runtime.migration] Context impl SQLiteImpl.
    INFO  [alembic.runtime.migration] Will assume non-transactional DDL.
    … +32 lines (ctrl + t to view transcript)
      }
• Updated Plan
  └ 真实 Provider 最小 smoke 前置工作已完成，并在干净工作树通过最终 readiness gate。
    ✔ 检查 Provider 接口、CLI、依赖与当前工作树
    ✔ 实现真实 Provider 适配器与结构化响应边界
    ✔ 接入 CLI 最小 smoke 模式并保持默认离线
    ✔ 增加全 mock 测试和秘密审计验证
    ✔ 运行 pytest、ruff、mypy 和离线 readiness 审计

• Ran git status --short && git log -2 --oneline
  └ 2240426 make readiness tests independent of worktree state
    4387fc0 add provider smoke readiness gate

──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• 已完成真实模型最小验证前置工作，未调用任何真实 API。

  实现内容：

  - OpenAI Responses API Provider 适配器
  - 结构化 tool-call 解析与验证
  - timeout、rate limit、provider error、JSON、预算等失败边界
  - token、成本、raw response、失败审计
  - Provider 失败时保留失败 Turn 与审计记录
  - API key 不进入请求、Manifest、数据库或日志
  - CLI 必须显式添加 --allow-external-provider 才允许联网
  - OpenAI 默认 smoke 限制：
      - 1 turn
      - 每轮最多 2 次 Provider 调用
      - 每轮最多 1 次工具调用
      - 最大成本 $0.25

  验证结果：

  - pytest: 79 passed
  - ruff: passed
  - mypy: passed
  - migration/replay/state hash/audit diff: passed
  - git_dirty=false
  - ready_for_real_provider=true

  提交：

  - 4387fc0 add provider smoke readiness gate
  - 2240426 make readiness tests independent of worktree state

  当前工作树干净，系统现在可以执行一次受限真实模型 smoke test。

─ Worked for 3m 28s ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────


› 不接入openai api，接入豆包api，代码需要更改吗


■ You've hit your usage limit. Upgrade to Pro (https://chatgpt.com/explore/pro), visit https://chatgpt.com/codex/settings/usage to
purchase more credits or try again at 5:45 PM.


› Find and fix a bug in @filename

  gpt-5.5 default · /workspaces/codespaces-blank