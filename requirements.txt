# Emergence World mechanism-level reproduction
# Target runtime: Python 3.12
#
# This file lists dependencies needed across the implementation phases in
# IMPLEMENTATION_PLAN.md. SQLite itself is provided by Python's standard
# library; no separate sqlite package is required.

# Core data models, validation, and configuration
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
PyYAML>=6.0,<7
jsonschema>=4.22,<5

# SQLite persistence and schema migrations
SQLAlchemy>=2.0,<3
alembic>=1.13,<2
aiosqlite>=0.20,<1

# CLI and human-readable inspection output
typer>=0.12,<1
rich>=13.7,<15

# Deterministic scheduling, timestamps, retries, and structured logs
APScheduler>=3.10,<4
python-dateutil>=2.9,<3
tenacity>=8.2,<10
structlog>=24.1,<26

# Provider-neutral HTTP integration and LLM provider adapters
httpx>=0.27,<1
openai>=1.30,<3
anthropic>=0.28,<1
google-genai>=1.0,<2

# Token-budget estimation for context assembly
tiktoken>=0.7,<1

# Future FastAPI service and Railway deployment layer
fastapi>=0.111,<1
uvicorn[standard]>=0.30,<1

# AWI metric calculation and experiment analysis
numpy>=1.26,<3
pandas>=2.2,<4
networkx>=3.3,<4

# Testing, async testing, coverage, and property-based invariant checks
pytest>=8.2,<10
pytest-asyncio>=0.23,<2
pytest-cov>=5,<8
hypothesis>=6.100,<7

# Development quality checks
ruff>=0.4,<1
mypy>=1.10,<2
